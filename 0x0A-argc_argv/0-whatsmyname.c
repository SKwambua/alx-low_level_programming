#include <stdio.h>
/**
 * main - prints its name followed by an new line
 * @argc: number of arguments
 * @argv: array of arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = argc;

	printf("%s\n", *argv);
	return (0);
}
