#include "main.h"
/**
 * _islower - checks for lowercase characters
 * @c: the character to check
 * Return: If c is lowercase 1, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
